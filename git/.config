[core]
	editor = vim

[user]
	email = iluhaar@gmail.com
	name = iluhaar

[branch]
        sort = -committerdate

[alias]
	new = "!f() { \\\n  if [ \"$1\" = \"-c\" ]; then \\\n    if [ -z \"$2\" ]; then \\\n      echo \"Usage: git new -c <ticket-number>\"; \\\n      return 1; \\\n    fi; \\\n    folder=$(basename \"$PWD\"); \\\n    branch=\"${folder}-$2\"; \\\n  else \\\n    if [ -z \"$1\" ]; then \\\n      echo \"Usage: git new [-c] <branch-name-or-ticket>\"; \\\n      return 1; \\\n    fi; \\\n    branch=\"$1\"; \\\n  fi; \\\n  git checkout -b \"$branch\"; \\\n}; f"
	f-diff = git diff --stat
	st = status
	contributors = shortlog -sne --group=author --group=trailer:co-authored-by
	upc = commit --amend --no-edit
	b = switch -
[blame]
        coloring = repeatedLines

[credential]
        helper = manager
        useHttpPath = true
        credentialStore = secretservice

[alias]
	new = switch -c
	f-diff = git diff --stat
	st = status
	contributors = shortlog -sne --group=author --group=trailer:co-authored-by

[blame]
	coloring = repeatedLines
